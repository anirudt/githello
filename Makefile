default:
	@echo "Hi, there!"
	touch ~/me.py
	mkdir -p /etc/minio-python
	cp hello.py /etc/minio-python/

hello:
	@echo "Hi, there!"
	touch ~/me.py
	mkdir -p /etc/minio-python
	cp hello.py /etc/minio-python/

all:
	@echo "Hi, there!"
	touch ~/me.py
	mkdir -p /etc/minio-python
	cp hello.py /etc/minio-python/

install:
	@echo "Hi, there!"
	touch ~/me.py
	mkdir -p /etc/minio-python
	cp hello.py /etc/minio-python/

